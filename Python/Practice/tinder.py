setInterval(
    function () {
        const elem = document.getElementsByClassName("recsGamepad__button");
        elem[3].click();
    }, 1000)

1アロー関数で定義
2DOM操作したものを変数に
3バリデーションもかけよう

const pushLikeButton =

setInterval(pushLikeButton,1000)
